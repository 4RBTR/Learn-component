// File: next.config.ts

/** @type {import('next').NextConfig} */
const nextConfig = {
  // Konfigurasi gambar eksternal sudah tidak diperlukan lagi,
  // jadi biarkan kosong atau hapus blok 'images'.
};

export default nextConfig;